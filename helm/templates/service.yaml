apiVersion: v1
kind: Service
metadata:
  name: {{ include "mini-platform-app.fullname" . }}
  labels:
    {{- include "mini-platform-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mini-platform-app.selectorLabels" . | nindent 4 }}
